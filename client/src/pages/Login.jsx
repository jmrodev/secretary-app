import React from 'react';
import LoginForm from '../features/auth/components/LoginForm';

/**
 * Login Page Orchestrator.
 * Pure orchestrator component that renders the LoginForm from the auth feature.
 */
const Login = () => {
    return <LoginForm />;
};

export default Login;
