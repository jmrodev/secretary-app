import React from 'react';
import { RegisterForm } from '../features/auth';

/**
 * Register Page Orchestrator.
 * Pure orchestrator component that renders the RegisterForm from the auth feature.
 */
const Register = () => {
    return <RegisterForm />;
};

export default Register;
