{
  "name": "secretary-app",
  "private": true,
  "scripts": {
    "lint": "npm --prefix client run lint",
    "precommit": "npm run lint && echo '🚀 Linter passed! Safe to commit.'",
    "build": "npm --prefix client run build",
    "test": "npm --prefix server run test"
  },

  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.0.1",
    "vite": "^8.0.10",
    "wrangler": "^4.87.0"
  },
  "pnpm": {
    "overrides": {
      "form-data": "^4.0.1",
      "qs": "^6.11.0",
      "follow-redirects": "^1.15.4",
      "tough-cookie": "^4.1.3"
    }
  }
}
